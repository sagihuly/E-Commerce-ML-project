# E-Commerce-ML-project

In the following project, we will explore data from an E-Commerce store sessions, use it to train several machine learning models and then output the prediction as a CSV file. Using these models, we will try to predict whether a new session will end up with a purchase or not. 
